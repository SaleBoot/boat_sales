# 运行数据说明

本目录为可复现的完整网页端源码和模型资源。

为避免交付包泄露生产账号、会话、客户联系方式、用户图纸及测试输出，以下运行时内容没有随源码复制：

- `users.json`
- `uploads/`
- `output/`
- `.env` 及其他本机环境配置

启动服务后，必要目录和本地开发账号文件会由程序按需创建。生产环境请使用 `DATABASE_URL` 或标准 PostgreSQL 环境变量连接正式数据库，并由管理员单独设置正式凭据。
